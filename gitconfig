[core]
	editor = vim
	pager = less
[user]
	name = Richard Ma
	email = richard.ma.19850509@gmail.com
	signingkey = 40D91374
[github]
	user = richard-ma
	token = 55e3f4eb9d3ec2db788921d6bd0c9697
[alias]
	st = status
	ci = commit
    co = checkout
	wc = whatchanged
    br = branch
    pom = push -v origin master
    pom = push -v origin master --tags
    fu = fetch -v origin master:update
    mu = merge -v update
    du = branch -d update
    pod = push -v origin dev
    pod = push -v origin dev --tags
    fu = fetch -v origin dev:update-dev
    mu = merge -v update-dev
    du = branch -d update-dev
[diff]
	renamelimit = 0
[merge]
    tool = vimdiff
[mergetool "vimdiff"]
    cmd = "vim --noplugin \"$PWD/$MERGED\" \
        +\":split $PWD/$REMOTE\" +\":set buftype=nowrite\" \
        +\":vertical diffsplit $PWD/$LOCAL\" +\":set buftype=nowrite\" \
        +\":vertical diffsplit $PWD/$BASE\" +\":set buftype=nowrite\" \
        +\":wincmd l\""
[color]
    ui = true
	diff = auto
	interactive = auto
	status = auto
	branch = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[gc]
	auto = 1
